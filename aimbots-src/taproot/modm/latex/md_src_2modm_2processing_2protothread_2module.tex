\chapter{Protothreads }
\hypertarget{md_src_2modm_2processing_2protothread_2module}{}\label{md_src_2modm_2processing_2protothread_2module}\index{Protothreads@{Protothreads}}
\label{md_src_2modm_2processing_2protothread_2module_autotoc_md20}%
\Hypertarget{md_src_2modm_2processing_2protothread_2module_autotoc_md20}%


Protothreads are extremely lightweight stackless threads designed for severely memory constrained systems, such as small embedded systems or wireless sensor network nodes. Protothreads provide linear code execution for event-\/driven systems implemented in C. Protothreads can be used with or without an underlying operating system to provide blocking event-\/handlers.

Protothreads provide sequential flow of control without complex state machines or full multi-\/threading.

Since they implement some kind of cooperative multi-\/threading, Protothreads are non-\/preemptable. Therefore, a context switch can only take place on blocking operations, which means you don\textquotesingle{}t need complex synchronization.

Protothreads are also stackless, so local variables are not preserved across context switches, and must instead become member variables of the {\ttfamily modm::\+\+Protothread} subclass

A protothread runs within a single function ({\ttfamily modm::\+\+Protothread::\+run()}) and cannot span over other functions. A protothread may call normal functions, but cannot block inside a called function. Blocking inside nested function calls is instead made by spawning a separate protothread for each potentially blocking function.

The protothread concept was developed by Adam Dunkels and Oliver Schmidt:\+ \href{http://dunkels.com/adam/pt}{\texttt{http:\+/\+/\+dunkels.\+com/\+adam/\+pt}}

Originally ported to C++ for use by Hamilton Jet (www.\+hamiltonjet.\+co.\+nz) by Ben Hoyt, but stripped down for public release.\hypertarget{md_src_2modm_2processing_2protothread_2module_autotoc_md21}{}\doxysection{\texorpdfstring{Example}{Example}}\label{md_src_2modm_2processing_2protothread_2module_autotoc_md21}

\begin{DoxyCode}{0}
\DoxyCodeLine{\textcolor{preprocessor}{\#include\ <modm/processing/protothread.hpp>}}
\DoxyCodeLine{}
\DoxyCodeLine{\textcolor{keyword}{using\ }Led\ =\ GpioB0;}
\DoxyCodeLine{}
\DoxyCodeLine{\textcolor{keyword}{class\ }BlinkingLight\ :\ \textcolor{keyword}{public}\ \doxymbox{\hyperlink{classmodm_1_1pt_1_1_protothread}{modm::pt::Protothread}}}
\DoxyCodeLine{\{}
\DoxyCodeLine{\textcolor{keyword}{public}:}
\DoxyCodeLine{\ \ \ \ \textcolor{keywordtype}{bool}}
\DoxyCodeLine{\ \ \ \ run()}
\DoxyCodeLine{\ \ \ \ \{}
\DoxyCodeLine{\ \ \ \ \ \ \ \ PT\_BEGIN();}
\DoxyCodeLine{}
\DoxyCodeLine{\ \ \ \ \ \ \ \ \textcolor{comment}{//\ set\ everything\ up}}
\DoxyCodeLine{\ \ \ \ \ \ \ \ Led::setOutput();}
\DoxyCodeLine{\ \ \ \ \ \ \ \ Led::set();}
\DoxyCodeLine{}
\DoxyCodeLine{\ \ \ \ \ \ \ \ \textcolor{keywordflow}{while}\ (\textcolor{keyword}{true})}
\DoxyCodeLine{\ \ \ \ \ \ \ \ \{}
\DoxyCodeLine{\ \ \ \ \ \ \ \ \ \ \ \ timeout.restart(100ms);}
\DoxyCodeLine{\ \ \ \ \ \ \ \ \ \ \ \ Led::set();}
\DoxyCodeLine{\ \ \ \ \ \ \ \ \ \ \ \ PT\_WAIT\_UNTIL(timeout.isExpired());}
\DoxyCodeLine{}
\DoxyCodeLine{\ \ \ \ \ \ \ \ \ \ \ \ timeout.restart(200ms);}
\DoxyCodeLine{\ \ \ \ \ \ \ \ \ \ \ \ Led::reset();}
\DoxyCodeLine{\ \ \ \ \ \ \ \ \ \ \ \ PT\_WAIT\_UNTIL(timeout.isExpired());}
\DoxyCodeLine{\ \ \ \ \ \ \ \ \}}
\DoxyCodeLine{}
\DoxyCodeLine{\ \ \ \ \ \ \ \ PT\_END();}
\DoxyCodeLine{\ \ \ \ \}}
\DoxyCodeLine{}
\DoxyCodeLine{\textcolor{keyword}{private}:}
\DoxyCodeLine{\ \ \ \ modm::ShortTimeout\ timeout;}
\DoxyCodeLine{\};}
\DoxyCodeLine{\textcolor{comment}{//\ ...}}
\DoxyCodeLine{}
\DoxyCodeLine{BlinkingLight\ light;}
\DoxyCodeLine{}
\DoxyCodeLine{\textcolor{keywordflow}{while}\ (\textcolor{keyword}{true})\ \{}
\DoxyCodeLine{\ \ \ \ light.run();}
\DoxyCodeLine{\}}

\end{DoxyCode}
 