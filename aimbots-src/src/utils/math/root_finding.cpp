#include "utils/common_types.hpp"
#include <vector>

//take in a function to use to compute data and find roots

float root_finder(){
    //list of roots
    vector<float> root_candidates;

    //pick a random point, this can be optimized based on the kinds of inputs that are normal
    //find the slope at that point, this is done using two points of a varying distance, this can be optimized
    //create a line that starts at the inital point and uses the slope to arrivel
}

//return a single root as the output
