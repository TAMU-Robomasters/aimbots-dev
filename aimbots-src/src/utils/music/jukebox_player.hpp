#pragma once

#include "utils/common_types.hpp"

namespace utils::jukebox {

enum SongTitle { PacMan = 0, WeAreNumberOne, ChainSawMan, Mystery, CrabRave, Zelda };

class Jukebox {
public:
    // Jukebox(src::Drivers* drivers);
    ~Jukebox() = default;

private:
};
}  // namespace utils::jukebox